const LS_PRODUCTS='starpos_products_v1',LS_SALES='starpos_sales_v1';let cart=[];const money=n=>'₺'+Number(n||0).toLocaleString('tr-TR',{minimumFractionDigits:2,maximumFractionDigits:2});const products=()=>JSON.parse(localStorage.getItem(LS_PRODUCTS)||'[]');const saveProducts=a=>localStorage.setItem(LS_PRODUCTS,JSON.stringify(a));const sales=()=>JSON.parse(localStorage.getItem(LS_SALES)||'[]');const saveSales=a=>localStorage.setItem(LS_SALES,JSON.stringify(a));function seed(){if(!products().length){saveProducts([{id:crypto.randomUUID(),name:'Coca Cola 330 ml',barcode:'8690637891234',buy:15,sell:25,stock:48,min:10},{id:crypto.randomUUID(),name:'Saka Su 1.5 L',barcode:'8690749100012',buy:5,sell:10,stock:72,min:15},{id:crypto.randomUUID(),name:'Ülker Çikolata 80 gr',barcode:'8690504021336',buy:18,sell:28,stock:36,min:8}])}}function render(){const ps=products(),ss=sales();document.getElementById('productCount').textContent=ps.length;document.getElementById('lowStock').textContent=ps.filter(p=>Number(p.stock)<=Number(p.min)).length;document.getElementById('saleCount').textContent=ss.length;document.getElementById('dailyTotal').textContent=money(ss.reduce((t,s)=>t+s.total,0));document.getElementById('productList').innerHTML=ps.map(p=>`<div class="product"><div><b>${p.name}</b><br><small>${p.barcode}</small></div><div><b>${money(p.sell)}</b><br><small>Stok: ${p.stock}</small></div></div>`).join('');const sel=document.getElementById('labelProduct');sel.innerHTML='<option value="">Ürün seçiniz</option>'+ps.map(p=>`<option value="${p.id}">${p.name}</option>`).join('');document.getElementById('lastOps').innerHTML=ss.length?ss.slice(-5).reverse().map(s=>`<div class="cartitem"><span>${s.date}</span><b>${money(s.total)}</b></div>`).join(''):'Henüz işlem yok';renderCart()}function renderCart(){document.getElementById('cartList').innerHTML=cart.length?cart.map((i,idx)=>`<div class="cartitem"><div><b>${i.name}</b><br><small>${i.barcode} x ${i.qty}</small></div><button onclick="removeCart(${idx})">Sil</button></div>`).join(''):'Sepet boş';document.getElementById('cartTotal').textContent=money(cart.reduce((t,i)=>t+i.sell*i.qty,0))}function addByBarcode(code){const p=products().find(x=>x.barcode==code.trim());if(!p){alert('Ürün bulunamadı. Ürünler ekranından ekleyebilirsiniz.');return}const c=cart.find(x=>x.id===p.id);c?c.qty++:cart.push({...p,qty:1});renderCart()}window.removeCart=i=>{cart.splice(i,1);renderCart()};document.querySelectorAll('nav button').forEach(b=>b.onclick=()=>{document.querySelectorAll('nav button').forEach(x=>x.classList.remove('active'));b.classList.add('active');document.querySelectorAll('.screen').forEach(s=>s.classList.remove('active'));document.getElementById('screen-'+b.dataset.target).classList.add('active')});document.getElementById('barcodeInput').addEventListener('keydown',e=>{if(e.key==='Enter'){addByBarcode(e.target.value);e.target.value=''}});document.getElementById('saveProduct').onclick=()=>{const p={id:crypto.randomUUID(),name:pName.value,barcode:pBarcode.value,buy:+pBuy.value,sell:+pSell.value,stock:+pStock.value,min:+pMin.value};if(!p.name||!p.barcode||!p.sell)return alert('Ürün adı, barkod ve satış fiyatı zorunlu');saveProducts([p,...products()]);pName.value=pBarcode.value=pBuy.value=pSell.value=pStock.value=pMin.value='';render()};document.getElementById('payBtn').onclick=()=>{if(!cart.length)return alert('Sepet boş');const ps=products();cart.forEach(c=>{const p=ps.find(x=>x.id===c.id);if(p)p.stock=Math.max(0,Number(p.stock)-c.qty)});saveProducts(ps);saveSales([...sales(),{date:new Date().toLocaleString('tr-TR'),total:cart.reduce((t,i)=>t+i.sell*i.qty,0),items:cart}]);cart=[];alert('Satış tamamlandı, stoktan düşüldü.');render()};document.getElementById('labelProduct').onchange=e=>{const p=products().find(x=>x.id===e.target.value);document.getElementById('labelPreview').innerHTML=p?`<b>${p.name}</b><span>${p.barcode}</span><div class="barcode">||||||||||||||||</div><strong>${money(p.sell)}</strong>`:'<b>StarPOS</b><span>Ürün seçiniz</span><div class="barcode">||||||||||||||||</div><strong>₺0,00</strong>'};document.getElementById('printLabel').onclick=()=>window.print();document.getElementById('clearData').onclick=()=>{if(confirm('Demo verileri silinsin mi?')){localStorage.clear();cart=[];render()}};document.getElementById('cameraBtn').onclick=async()=>{cameraBox.classList.remove('hidden');try{video.srcObject=await navigator.mediaDevices.getUserMedia({video:{facingMode:'environment'}})}catch(e){alert('Kamera açılamadı: '+e.message)}};document.getElementById('stopCamera').onclick=()=>{cameraBox.classList.add('hidden');if(video.srcObject)video.srcObject.getTracks().forEach(t=>t.stop())};let deferredPrompt;window.addEventListener('beforeinstallprompt',e=>{e.preventDefault();deferredPrompt=e;installBtn.hidden=false});installBtn.onclick=()=>deferredPrompt?.prompt();if('serviceWorker'in navigator)navigator.serviceWorker.register('service-worker.js');seed();render();
